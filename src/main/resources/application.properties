# Database Configuration (Use variaveis de ambiente ou perfis especificos)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/agendamento}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=false
spring.flyway.schemas=public
spring.flyway.validate-on-migrate=false

# Server Configuration
server.port=8081

# Profile Configuration - Usar 'dev' para desenvolvimento
spring.profiles.active=dev

# Additional Configuration
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never

# Email Configuration - OP??O 1: Gmail Real
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=SEU_EMAIL_REAL@gmail.com
# spring.mail.password=SUA_SENHA_DE_APLICATIVO_REAL
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# Email Configuration - OP??O 2: Servidor SMTP Fake para Desenvolvimento
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

